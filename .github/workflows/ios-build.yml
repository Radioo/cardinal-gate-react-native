name: Build iOS

on:
  push:
    branches:
      - master
  workflow_dispatch:
    inputs:
      configuration:
        description: Build configuration
        required: true
        default: Release
        type: choice
        options:
          - Debug
          - Release

jobs:
  build:
    runs-on: macos-latest
    env:
      CONFIGURATION: ${{ inputs.configuration || 'Release' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Cache node_modules
        id: cache-modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

      - name: Install dependencies
        if: steps.cache-modules.outputs.cache-hit != 'true'
        run: npm ci

      - name: Calculate native fingerprint
        id: fingerprint
        run: |
          HASH=$(npx @expo/fingerprint fingerprint:generate --platform ios | python3 -c "import json,sys; print(json.load(sys.stdin)['hash'])")
          echo "hash=$HASH" >> $GITHUB_OUTPUT

      - name: Expo Prebuild
        run: npx expo prebuild --platform ios

      - name: Cache CocoaPods
        id: cache-pods
        uses: actions/cache@v5
        with:
          path: ios/Pods
          key: pods-${{ hashFiles('ios/Podfile.lock') }}

      - name: Install CocoaPods
        if: steps.cache-pods.outputs.cache-hit != 'true'
        run: cd ios && pod install

      - name: Cache DerivedData
        uses: actions/cache@v5
        with:
          path: DerivedData
          key: ios-derived-data-${{ env.CONFIGURATION }}-${{ steps.fingerprint.outputs.hash }}-${{ github.sha }}
          restore-keys: |
            ios-derived-data-${{ env.CONFIGURATION }}-${{ steps.fingerprint.outputs.hash }}-

      - name: Build Archive (Unsigned)
        run: |
          xcodebuild -workspace ios/CARDINALGATE.xcworkspace \
            -scheme CARDINALGATE \
            -configuration ${{ env.CONFIGURATION }} \
            -derivedDataPath DerivedData \
            -archivePath ios/buildArchive/CARDINALGATE.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            archive

      - name: Upload build logs on failure
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: xcodebuild-logs
          path: DerivedData/Logs/Build/

      - name: Prepare IPA Package
        run: |
          COMMIT_ID=$(git rev-parse --short HEAD)
          DATE=$(date +"%Y-%m-%d_%H-%M")
          CONFIG=${{ env.CONFIGURATION }}

          mkdir -p Payload
          cp -r ios/buildArchive/CARDINALGATE.xcarchive/Products/Applications/CARDINALGATE.app Payload/
          zip -qr CARDINALGATE_${CONFIG}_${DATE}_${COMMIT_ID}.ipa Payload

          echo "IPA_NAME=CARDINALGATE_${CONFIG}_${DATE}_${COMMIT_ID}.ipa" >> $GITHUB_ENV

      - name: Upload Artifact
        uses: actions/upload-artifact@v7
        with:
          archive: false
          name: ${{ env.IPA_NAME }}
          path: ${{ env.IPA_NAME }}
