// Named re-export: platform shim must match react-native's named export API.
// The web variant (useColorScheme.web.ts) provides a custom implementation.
export { useColorScheme } from 'react-native';
